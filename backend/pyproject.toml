[project]
name = "backend"
version = "0.1.0"
description = "This is the backend for the project"
readme = "README.md"
requires-python = ">=3.14"
dependencies = ["mcp","pydantic","openai","httpx","dotenv"]
